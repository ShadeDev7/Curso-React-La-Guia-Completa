const sumar = (a = 0, b = 0) => a + b;

// export default sumar; // Para exportar solo una función

const restar = (a = 0, b = 0) => a - b;

// Para exportar múltiples funciones
export { sumar, restar };
